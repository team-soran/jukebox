jukebox
~~~~~~~~

jukebox on your laptop.


Installation
---------------

.. code-block::

   $ mkdir -p "$(pwd)/.git/hooks"
   $ ln -s "$(pwd)/pre-commit" "$(pwd)/.git/hooks/pre-commit"
   $ npm install
